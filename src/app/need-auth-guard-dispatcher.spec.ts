import { NeedAuthGuardDispatcher } from './need-auth-guard-dispatcher';

describe('NeedAuthGuardDispatcher', () => {
  it('should create an instance', () => {
    expect(new NeedAuthGuardDispatcher()).toBeTruthy();
  });
});
