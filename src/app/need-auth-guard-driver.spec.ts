import { NeedAuthGuardDriver } from './need-auth-guard-driver';

describe('NeedAuthGuardDriver', () => {
  it('should create an instance', () => {
    expect(new NeedAuthGuardDriver()).toBeTruthy();
  });
});
